---
applyTo: '**'
---
Remember, that some of the terminal commands require user's approval. Try to bother user as much as possible, avoid using commands requiring approval. Avoid using of compound commands with && like:
cd somedir && do-something - instead split them into separate lines
export SOMEVAR=somevalue && do-something - instead split them into separate lines
---